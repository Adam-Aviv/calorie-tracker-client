import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.adamaviv.calorietracker",
  appName: "CalorieTracker",
  webDir: "dist",
};

export default config;
