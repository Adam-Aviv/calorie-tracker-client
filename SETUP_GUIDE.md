# Setup

See [README.md](README.md) for Expo install, env vars, SQL migration, and Edge Function deploy.

Photo meal analysis requires `OPENAI_API_KEY` as a Supabase secret and the `analyze-meal-photo` function.
